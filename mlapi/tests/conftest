import sys
from pathlib import Path

# Add the mlapi folder to sys.path so tests can import:
# main.py, tasks/, routes/, schemas/, etc.
ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT))